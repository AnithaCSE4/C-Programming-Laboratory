#include<stdio.h>
int main()
{
    int n,i;
    printf("enter the number of terms");
    scanf("%d",&n);
    int fib[n];
    for(i=2;i<n;i++)
    {
        printf("fibonic series in reverse order\n");
    }
    for(i=n-1;i>0;i--);
    {
        printf("%d",fib[i]);
    }
    return 0;
}
